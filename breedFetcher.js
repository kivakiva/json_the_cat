const request = require('request');
const arg = process.argv.slice(2);



const fetchBreedDescription = function(breedName, callback(err, desc)) {
if (error) {
  callback(error)
} else {callback()}



}


module.exports = { fetchBreedDescription };